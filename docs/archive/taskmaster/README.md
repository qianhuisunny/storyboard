# Taskmaster Archive

These files were migrated out of `.taskmaster/` so useful PRDs and backlog notes remain available without tracking Taskmaster's local tool state.

The active `.taskmaster/` directory is ignored and should be treated as local workspace metadata.
